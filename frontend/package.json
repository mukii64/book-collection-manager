{
    "name": "frontend",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "concurrently \"yarn frontend\" \"yarn backend\"",
        "frontend": "vite",
        "backend": "cd ../backend && php artisan serve",
        "build": "vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "lit": "^3.2.1"
    },
    "devDependencies": {
        "concurrently": "^9.1.2",
        "vite": "^6.2.0"
    }
}
